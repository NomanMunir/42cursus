# Built-in Commands Test Cases
# Test all minishell built-in commands

# Echo tests
echo "Testing echo built-in"
echo -n "No newline test"
echo ""
echo -n
echo hello world from echo

# CD tests
pwd
cd /tmp
pwd
cd ..
pwd
cd ~
pwd
cd -
pwd

# Export tests
export TEST_VAR=hello
echo $TEST_VAR
export PATH_BACKUP=$PATH
export CUSTOM_VAR="custom value"
echo $CUSTOM_VAR
export

# Unset tests
unset TEST_VAR
echo $TEST_VAR
unset CUSTOM_VAR
echo $CUSTOM_VAR

# Env tests
env | grep USER
env | grep PATH | head -1

# Exit test (should be last)
exit
