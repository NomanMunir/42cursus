# Error Cases Test Cases
# Test error handling and recovery

# Command not found errors
nonexistent_command
echo "Still working after command error"

another_fake_command
echo "Another recovery test"

# File not found errors
cat nonexistent_file.txt
echo "Recovery after file error"

ls /nonexistent/directory
echo "Recovery after directory error"

# Permission denied errors
cat /etc/shadow 2>/dev/null || echo "Permission denied handled"
echo "Continue after permission error"

# Syntax errors
echo hello |
echo "Recovery after pipe syntax error"

cat <
echo "Recovery after redirect syntax error"

echo hello >
echo "Recovery after output syntax error"

# Invalid built-in usage
cd /nonexistent/path 2>/dev/null || echo "CD error handled"
echo "Continue after CD error"

export 123INVALID=value 2>/dev/null || echo "Export error handled"
echo "Continue after export error"

# Multiple errors in sequence
fake_cmd1 2>/dev/null || echo "Error 1"
fake_cmd2 2>/dev/null || echo "Error 2"
fake_cmd3 2>/dev/null || echo "Error 3"
echo "All errors handled, shell still running"

exit
